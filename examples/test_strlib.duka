local s = require("strlib")

print(s.VERSION)

local nums = { 1, 2, 3, 4 }
print(s.join(nums, "-"))

local sq = s.map(nums, function(x) return x * x end)
print(s.join(sq, " "))

local triple = s.times(function(x) return x * 2 end, 3)
print(triple(5))
