[workspace]
members = [
    "frontend",
    "backend",
    "backend-wasm",
    "app",
    "shared",
    "cli",
    "dukao",
    "macros",
    "gc",
    "pipeline",
    "printer",
    "lib",
    "lsp"
]
resolver = "3"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
