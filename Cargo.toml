[workspace]
members = [
    "frontend",
    "backend-wasm",
    "backend",
    "shared",
    "cli",
    "macros",
    "gc",
    "pipeline",
    "visual",
    "lib",
    "lsp"
]
resolver = "3"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
