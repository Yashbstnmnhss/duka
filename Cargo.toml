[workspace]
members = [
    "frontend",
    "backend-wasm",
    "backend",
    "shared",
    "cli",
    "dukao",
    "macros",
    "gc",
    "pipeline",
    "lib",
    "lsp"
]
resolver = "3"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
